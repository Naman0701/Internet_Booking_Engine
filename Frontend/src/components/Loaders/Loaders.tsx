import { FC } from "react";
import './Loaders.scss';

/**
 * A loader that just loads at the center of the screen
 */
const Loaders: FC = () => {
  return <div className="loader"></div>;
};

export default Loaders;
